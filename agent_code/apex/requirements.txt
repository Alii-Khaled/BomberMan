# Overlord tournament requirements (Docker CPU image).
# Training runs on CUDA (L40S path, AMP default); inference stays CPU.
# callbacks.py is torch-optional with CPU fallback.
torch>=2.4.1
numpy>=2.5.2
pygame>=2.6.1
scikit-learn>=1.9.0
scipy>=1.18.1
tqdm>=4.70.0
